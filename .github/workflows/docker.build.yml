---

name: Docker Build

on:
  push:
    tags:
      - 'v*.*.*'

env:
  BUN_VERSION: '1.3.11'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
